class A {
    public int foo() {
        if (this.bar(1) < 2)
            this.bar(1);
        else
            this.bar(2);
        return 0;
    }
    public int bar(int a) {
        return a;
    }
}
