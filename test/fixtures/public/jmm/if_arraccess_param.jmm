class A {
    public int foo(int[] a) {
        if (a[1-1] < 2)
            this.bar(1);
        else
            this.bar(2);
        return 0;
    }
    public int bar(int a) {
        return a;
    }
}
