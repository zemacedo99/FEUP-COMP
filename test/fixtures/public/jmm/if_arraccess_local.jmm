class A {
    public int foo() {
        int[] a;
        a = new int[4];
        a[0] = 1;

        if (a[1-1] < 2)
            this.bar(1);
        else
            this.bar(2);
        return 0;
    }
    public int bar(int a) {
        return a;
    }
}
