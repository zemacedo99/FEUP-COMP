class A {
    public int foo() {
        int[] a;
        a = new int[4];

        if (a[1-1] + a[1+1+1] < a[5-2])
            this.bar(1);
        else
            this.bar(2);
        return 0;
    }
    public int bar(int a) {
        return a;
    }
}
